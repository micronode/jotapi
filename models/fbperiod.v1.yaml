title: fbperiod
type: object
x-examples: {}
properties:
  start:
    type: string
    format: date-time
  end:
    type: string
    format: date-time
  fbtype:
    type: string
    enum:
      - FREE
      - BUSY
      - BUSY-UNAVAILABLE
      - BUSY-TENTATIVE
    default: BUSY
  duration:
    type: string
    format: duration
    example: PT5H30M
